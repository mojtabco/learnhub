document.addEventListener("DOMContentLoaded", function () {
    // Find the element with id "apps-tab"
    const appsTab = document.getElementById("apps-tab");

    // Simulate a click event on the apps-tab
    if (appsTab) {
        appsTab.click();
    }
});
